[program:{{ celery_beat_name }}]
command={{ celery_scripts_dir }}/{{ celery_beat_name }}_start
autostart=true
autorestart=true
user={{ celery_user }}
stdout_logfile={{ celery_beat_log_file }}
redirect_stderr = true
; if rabbitmq is supervised, set its priority higher so it starts first
priority=999
