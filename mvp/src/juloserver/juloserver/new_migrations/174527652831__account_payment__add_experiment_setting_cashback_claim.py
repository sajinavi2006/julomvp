# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2025-04-21 23:02
from __future__ import unicode_literals

from django.db import migrations

from juloserver.minisquad.constants import ExperimentConst
from juloserver.julo.models import ExperimentSetting
from datetime import datetime, timedelta


def add_experiment_setting_for_cashback_claim(apps, schema_editor):
    start_date = datetime.now().date() + timedelta(days=1)  # 1 Day after deployment
    end_date = start_date + timedelta(days=365)
    ExperimentSetting.objects.create(
        is_active=False,
        is_permanent=False,
        code=ExperimentConst.CASHBACK_CLAIM_EXPERIMENT,
        name="Cashback Claim Experiment",
        start_date=start_date,
        end_date=end_date,
        schedule="",
        action="",
        type="collection",
        criteria={
            "claim_duration": 8,
            "account_id_tail": {"control": [1, 3, 5, 7, 9], "experiment": [0, 2, 4, 6, 8]},
        },
    )


class Migration(migrations.Migration):

    dependencies = []

    operations = [
        migrations.RunPython(add_experiment_setting_for_cashback_claim, migrations.RunPython.noop)
    ]
