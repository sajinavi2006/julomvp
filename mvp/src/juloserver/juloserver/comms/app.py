from __future__ import unicode_literals

from django.apps import AppConfig


class CommsConfig(AppConfig):
    """
    Configuration for the comms app.
    The sub-app is managed by the comms squad (Squad 3 as 2024)
    """
    name = 'juloserver.comms'
