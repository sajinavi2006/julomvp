# from celery.schedules import crontab

ACCOUNT_SCHEDULE = {
    # 'reactivate_account_after_suspended_task': {
    #     'task': 'juloserver.account.tasks.scheduled_tasks.reactivate_account_after_suspended_task'
    #     'schedule': crontab(minute=0, hour=5),
    # },
    # 'reactivation-account': {
    #     'task': 'juloserver.account.tasks.account_task.scheduled_reactivation_account',
    #     'schedule': crontab(minute=0, hour=2),  # new J1 account payment
    # },
}
