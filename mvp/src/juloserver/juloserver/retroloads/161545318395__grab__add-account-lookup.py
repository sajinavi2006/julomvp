# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-03-11 08:59
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import Workflow, Partner
from juloserver.account.models import AccountLookup


def add_account_lookup(apps, _schema_editor):
    partner = Partner.objects.filter(name="grab").last()
    workflow = Workflow.objects.filter(
        name="GrabWorkflow",
        is_active=True,
    ).last()
    if workflow:
        AccountLookup.objects.get_or_create(
            name='GRAB',
            workflow=workflow,
            payment_frequency='daily',
            partner=partner
        )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_account_lookup, migrations.RunPython.noop),
    ]
