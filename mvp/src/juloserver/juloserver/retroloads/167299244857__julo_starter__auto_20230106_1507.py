# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-01-06 08:07
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import ChangeReason, StatusLookup


def run(apps, schema_editor):
    status = StatusLookup.objects.filter(status_code=149).last()
    reason = "Application Julo Starter accepted"

    exists = ChangeReason.objects.filter(
        reason=reason,
        status=status
    ).exists()
    if not exists:
        ChangeReason.objects.create(
            reason=reason,
            status=status
        )


class Migration(migrations.Migration):
    dependencies = [
    ]

    operations = [
        migrations.RunPython(run, migrations.RunPython.noop),
    ]
