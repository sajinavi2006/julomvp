# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-12-21 06:40
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import Onboarding
from juloserver.julo.constants import OnboardingIdConst


def run(apps, schema_editor):
    """
    Add new onboarding_id = 9
    The new onboarding_id is for new Emergency Contact Form

    Refer to this ticket:
    https://juloprojects.atlassian.net/browse/RUS1-2536
    """
    new_onboarding_id = 9
    onboarding = Onboarding.objects.filter(id=new_onboarding_id).last()
    if onboarding:
        onboarding.update_safely(
            description="JULO One for LFS Split emergency contact",
            status=True,
        )
    else:
        Onboarding.objects.create(
            id=new_onboarding_id,
            description="JULO One for LFS Split emergency contact",
            status=True,
        )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(run, migrations.RunPython.noop),
    ]