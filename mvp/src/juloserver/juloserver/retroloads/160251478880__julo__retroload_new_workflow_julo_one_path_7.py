# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2020-10-12 14:59
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import Workflow, WorkflowStatusPath


def add_workflow_julo_one(apps, _schema_editor):
    workflow = Workflow.objects.get(
        name="JuloOneWorkflow"
    )
    # WorkflowStatusPath
    WorkflowStatusPath.objects.filter(
        status_previous=141,
        status_next=135,
        type="unhappy",
        workflow=workflow
    ).delete()
    WorkflowStatusPath.objects.filter(
        status_previous=141,
        status_next=137,
        type="unhappy",
        workflow=workflow
    ).delete()
    WorkflowStatusPath.objects.filter(
        status_previous=142,
        status_next=135,
        type="unhappy",
        workflow=workflow
    ).delete()
    WorkflowStatusPath.objects.filter(
        status_previous=142,
        status_next=137,
        type="unhappy",
        workflow=workflow
    ).delete()
    WorkflowStatusPath.objects.filter(
        status_previous=130,
        status_next=142,
        type="unhappy",
        workflow=workflow
    ).delete()
    WorkflowStatusPath.objects.filter(
        status_previous=150,
        status_next=145,
        type="unhappy",
        workflow=workflow
    ).update(type='detour')


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_workflow_julo_one, migrations.RunPython.noop)
    ]

