# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-02-01 06:43
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import (
    Workflow, WorkflowStatusPath, WorkflowStatusNode, ProductLine
)
from juloserver.julo.constants import WorkflowConst


def create_grab_status_path(workflow):
    WorkflowStatusPath.objects.get_or_create(
        status_previous=150,
        status_next=180,
        type="detour",
        workflow=workflow
    )

    WorkflowStatusPath.objects.get_or_create(
        status_previous=180,
        status_next=190,
        type="happy",
        workflow=workflow
    )

    WorkflowStatusPath.objects.get_or_create(
        status_previous=190,
        status_next=180,
        type="detour",
        workflow=workflow
    )


def create_grab_status_node(workflow):
    WorkflowStatusNode.objects.create(
        status_node=180,
        handler='Grab180Handler',
        workflow=workflow
    )


def add_grab_status_180(apps, _schema_editor):
    workflow = Workflow.objects.filter(name=WorkflowConst.GRAB).last()
    create_grab_status_node(workflow)
    create_grab_status_path(workflow)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_grab_status_180, migrations.RunPython.noop)
    ]
