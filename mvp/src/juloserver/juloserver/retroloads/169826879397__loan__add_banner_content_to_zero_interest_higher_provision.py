# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-10-25 21:19
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.constants import FeatureNameConst
from juloserver.julo.models import FeatureSetting

def add_banner_content_zero_interest_higher_provision(apps, _schema_editor):
    feature_setting = FeatureSetting.objects.filter(
        feature_name=FeatureNameConst.ZERO_INTEREST_HIGHER_PROVISION).last()
    if not feature_setting:
        return

    params = feature_setting.parameters
    params['content'] = {
        "title": 'Nikmati Transaksi dengan Bunga 0%',
        "banner_link": "https://statics.julo.co.id/zero_interest/banner_zero_interest.png",
        "description": "Setiap transaksi senilai maks. Rp3.000.000 dengan jangka waktu 1 sampai 3 bulan akan mendapatkan bunga 0%, lho!",
        "webview_link": "https://www.julo.co.id",
    }
    feature_setting.parameters = params
    feature_setting.save()


class Migration(migrations.Migration):
    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            add_banner_content_zero_interest_higher_provision,
            migrations.RunPython.noop
        ),
    ]

