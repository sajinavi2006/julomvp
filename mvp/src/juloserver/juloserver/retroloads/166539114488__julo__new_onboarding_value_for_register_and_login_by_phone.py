# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-10-10 08:39
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import Onboarding


def add_new_configuration_onboarding_login_register(apps, schema_editor):
    """
    Add new value for register & login by phone
    with ID and description:
    4: Register with phone number AND Long Form
    5: Register with phone number AND Long Form Shortened

    Refer to the card:
    https://juloprojects.atlassian.net/browse/RUS1-1287
    """

    new_rows = [
        {
            'id': 4,
            'description': 'Register with phone number AND Long Form'
        },
        {
            'id': 5,
            'description': 'Register with phone number AND Long Form Shortened'
        }
    ]

    for data in new_rows:
        is_exist = Onboarding.objects.filter(id=data['id']).exists()
        if not is_exist:
            Onboarding.objects.create(id=data['id'],
                                      description=data['description'],
                                      status=True)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_new_configuration_onboarding_login_register, migrations.RunPython.noop)
    ]
