# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-04-04 07:03
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import StatusLookup
from juloserver.julo.statuses import ApplicationStatusCodes


def run(apps, schema_editor):
    status_lookup = StatusLookup.objects.filter(
        status_code=ApplicationStatusCodes.ACTIVATION_AUTODEBET
    ).last()

    if status_lookup:
        status_lookup.status="Activation Autodebet",
        status_lookup.handler = None
        status_lookup.save()
    else:
        StatusLookup.objects.create(
            status_code=ApplicationStatusCodes.ACTIVATION_AUTODEBET,
            status="Activation Autodebet",
            handler=None,
        )


class Migration(migrations.Migration):
    dependencies = [
    ]

    operations = [
        migrations.RunPython(run, migrations.RunPython.noop),
    ]
