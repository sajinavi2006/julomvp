# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-12-16 07:44
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import FeatureSetting
from juloserver.julo.constants import FeatureNameConst


def update_feature_setting_digisign(apps, schema_editor):
    fs = FeatureSetting.objects.filter(feature_name=FeatureNameConst.DIGISIGN).last()

    if not fs:
        return

    # Load existing parameters
    params = fs.parameters

    # Ensure 'sign_position' and 'borrower' keys exist
    if 'sign_position' not in params:
        params['sign_position'] = {}
    if 'borrower' not in params['sign_position']:
        params['sign_position']['borrower'] = {}

    # Add the new borrower entry
    params['sign_position']['borrower']['axiata_web'] = {"page": 6, "pos_x": 20, "pos_y": 160}

    # Update the parameters field
    fs.parameters = params
    fs.save()


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(update_feature_setting_digisign, migrations.RunPython.noop)]
