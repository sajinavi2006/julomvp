# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-02-12 15:45
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import StatusLookup


def load_status_lookups_credit_card(apps, schema_editor):
    status_lookups = [

        # Credit Card Application Status

        (505, 'Card Out of Stock'),
        (510, 'Card Application Submitted'),
        (511, 'Card Verification Success'),
        (512, 'Card on Shipping'),
        (513, 'Card Application Rejected'),
        (514, 'Resubmit Selfie'),
        (515, 'Card Received by User'),
        (520, 'Card Activated'),
        (521, 'Card Blocked'),
        (522, 'Card Closed'),
        (523, 'Resubmission'),

    ]

    status_lookup_data = []
    for status_code, status in status_lookups:
        status_lookup_data.append(
            StatusLookup(
                status_code=status_code,
                status=status
            )
        )

    StatusLookup.objects.bulk_create(status_lookup_data, batch_size=25)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(load_status_lookups_credit_card),
    ]
