# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-11-28 16:16
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import Workflow, WorkflowStatusPath, WorkflowStatusNode


def add_workflow_overhaul_julo_one(apps, _schema_editor):

    workflow = Workflow.objects.get(
        name="JuloOneWorkflow"
    )
    # WorkflowStatusPath
    WorkflowStatusPath.objects.get_or_create(
        status_previous=120,
        status_next=135,
        type="graveyard",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=121,
        status_next=130,
        type="happy",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=130,
        status_next=140,
        type="happy",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=130,
        status_next=135,
        type="graveyard",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=140,
        status_next=141,
        type="happy",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=140,
        status_next=133,
        type="graveyard",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=141,
        status_next=175,
        type="detour",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=141,
        status_next=133,
        type="graveyard",
        workflow=workflow
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_workflow_overhaul_julo_one, migrations.RunPython.noop),
    ]
