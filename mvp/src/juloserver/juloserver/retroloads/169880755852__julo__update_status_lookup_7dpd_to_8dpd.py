# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-11-01 02:59
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import StatusLookup


def update_new_status_lookup_for_dpd_8(apps, schema_editor):
    status_lookups = [
        (StatusLookup.PAYMENT_8DPD_CODE, "8dpd"),
        (StatusLookup.LOAN_8DPD_CODE, "8dpd")
    ]
    
    for status_code, status in status_lookups:
        status_lookup = StatusLookup.objects.filter(status_code=status_code).first()
        if not status_lookup:
            StatusLookup.objects.create(status_code=status_code, status=status)
        else:
            if status_lookup.status != status: 
                status_lookup.status = status
                status_lookup.save()

class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(update_new_status_lookup_for_dpd_8,
            migrations.RunPython.noop),
    ]
