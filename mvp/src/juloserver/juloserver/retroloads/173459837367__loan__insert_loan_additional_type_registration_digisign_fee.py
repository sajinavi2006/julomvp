# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-12-19 08:52
from __future__ import unicode_literals

from django.db import migrations

from juloserver.loan.constants import LoanDigisignFeeConst
from juloserver.loan.models import LoanAdditionalFeeType


def insert_registration_digisign_fee_type_loan_additional_type(apps, _schema_editor):
    bulk_data = []
    bulk_data.append(
        LoanAdditionalFeeType(
            name=LoanDigisignFeeConst.REGISTRATION_DUKCAPIL_FEE_TYPE,
            notes=("Digisign registration fee for Dukcapil check")
        )
    )
    bulk_data.append(
        LoanAdditionalFeeType(
            name=LoanDigisignFeeConst.REGISTRATION_FR_FEE_TYPE,
            notes=("Digisign registration fee for FR check")
        )
    )
    bulk_data.append(
        LoanAdditionalFeeType(
            name=LoanDigisignFeeConst.REGISTRATION_LIVENESS_FEE_TYPE,
            notes=("Digisign registration fee for Liveness check")
        )
    )

    LoanAdditionalFeeType.objects.bulk_create(bulk_data)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            insert_registration_digisign_fee_type_loan_additional_type,
            migrations.RunPython.noop
        ),
    ]
