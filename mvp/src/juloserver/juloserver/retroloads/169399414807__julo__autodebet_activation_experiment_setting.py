# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-09-06 09:55
from __future__ import unicode_literals

from dateutil.relativedelta import relativedelta

from django.db import migrations
from django.utils import timezone

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def add_experiment_setting_autodebet_activation(apps, schema_editor):
    current_datetime = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.AUTODEBET_ACTIVATION_EXPERIMENT,
        name="Autodebet Activation Experiment",
        start_date=current_datetime,
        end_date=current_datetime + relativedelta(years=1),
        schedule="",
        action='{"count": 0}',
        type="underwriting",
        criteria={
            "bottom_threshold": 0.75,
            "limit": 2500,
        }
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_experiment_setting_autodebet_activation, migrations.RunPython.noop)
    ]
