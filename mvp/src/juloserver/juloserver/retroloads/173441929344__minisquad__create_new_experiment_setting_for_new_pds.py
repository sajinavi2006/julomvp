# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-12-17 07:08
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from dateutil.relativedelta import relativedelta
from juloserver.julo.models import ExperimentSetting
from juloserver.minisquad.constants import ExperimentConst


def add_new_pds_experiment_setting(apps, schema_editor):
    now = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.NEW_PDS,
        name="New PDS",
        start_date=now,
        end_date=now + relativedelta(days=1),
        schedule="",
        action="",
        type="collection",
        criteria={'delay_upload_function': 15},  # in minutes
    )


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(add_new_pds_experiment_setting, migrations.RunPython.noop)]
