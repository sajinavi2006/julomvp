# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2025-05-13 08:29
from __future__ import unicode_literals

from django.utils import timezone

from django.db import migrations
import random
import string
from django.contrib.auth.models import User

from juloserver.followthemoney.constants import LoanAgreementType
from juloserver.followthemoney.services import generate_lender_signature
from juloserver.julo.models import (
    ProductProfile,
    LenderDisburseCounter,
    LenderProductCriteria,
    Partner,
    FeatureSetting,
)
from juloserver.followthemoney.models import (
    LenderCurrent,
    LenderBalanceCurrent,
    LenderBankAccount,
    LoanAgreementTemplate,
    LenderApproval,
)
from juloserver.julo.constants import (
    FeatureNameConst,
    ProductLineCodes,
)

lender_name = "iaf_jtp"
poc_email = "hans@julofinance.com"
signature_key = "lender"


def create_iaf_jtp_lender(apps, schema_editor):
    jtp = LenderCurrent.objects.filter(lender_name="jtp").last()
    if not jtp:
        return
    jtp_partner = jtp.user.partner

    user = User.objects.filter(username=lender_name).first()
    if not user:
        alphabet = string.ascii_letters + string.digits
        password = ''.join(random.choice(alphabet) for _ in range(8))
        user = User.objects.create_user(lender_name, poc_email, password)

    partner = Partner.objects.get_or_none(user=user)
    if not partner:
        partner = Partner.objects.create(
            user=user,
            name=lender_name,
            type="lender",
            email=user.email,
            is_active=False,
            poc_name=jtp_partner.poc_name,
            poc_email=user.email,
            poc_phone=jtp_partner.poc_phone,
            source_of_fund=jtp_partner.source_of_fund,
            company_name=jtp_partner.company_name,
            company_address=jtp_partner.company_address,
            business_type=jtp_partner.business_type,
            agreement_letter_number=jtp_partner.agreement_letter_number,
        )

        lender = LenderCurrent.objects.create(
            user=user,
            lender_name=lender_name,
            lender_address=jtp.lender_address,
            business_type=jtp.business_type,
            poc_email=user.email,
            poc_name=jtp.poc_name,
            poc_phone=jtp.poc_phone,
            poc_position=jtp.poc_position,
            source_of_fund=jtp.source_of_fund,
            lender_display_name=lender_name,
            service_fee=0,
            lender_status="inactive",
            addendum_number=jtp.addendum_number,
            insurance=jtp.insurance,
            pks_number=jtp.pks_number,
            xfers_token=jtp.xfers_token,
            is_low_balance_notification=True,
            minimum_balance=jtp.minimum_balance,
            license_number=jtp.license_number,
            company_name=jtp.company_name,
            is_manual_lender_balance=True,
            is_only_escrow_balance=True,
        )

        LenderDisburseCounter.objects.create(lender=lender, partner=partner)
        LenderBalanceCurrent.objects.create(lender=lender)
        LenderProductCriteria.objects.create(
            lender=lender,
            partner=partner,
            type='Product List',
            product_profile_list=list(
                ProductProfile.objects.filter(
                    code__in=list(ProductLineCodes.DANA_PRODUCT)
                ).values_list('id', flat=True)
            ),
        )

        jtp_bank_accounts = jtp.lenderbankaccount_set.filter(bank_account_status="active")
        banks = []
        for bank_account in jtp_bank_accounts:
            bank_account.pk = None
            bank_account.lender = lender
            bank_account.name_bank_validation = None
            banks.append(bank_account)
        LenderBankAccount.objects.bulk_create(banks)

        agreement_templates = []
        for agreement_type in LoanAgreementType.LIST:
            base_agreement_template = LoanAgreementTemplate.objects.filter(
                agreement_type=agreement_type,
                lender=jtp,
            ).last()
            if base_agreement_template:
                body = base_agreement_template.body
                if agreement_type == LoanAgreementType.SKRTP:
                    body = body.replace('<div>Direktur</div>', '<div>{{ poc_position }}</div>')

                agreement_templates.append(
                    LoanAgreementTemplate(
                        lender=lender,
                        body=body,
                        is_active=True,
                        agreement_type=agreement_type,
                    )
                )
        LoanAgreementTemplate.objects.bulk_create(agreement_templates)

        if jtp_partner.lenderapproval:
            LenderApproval.objects.create(
                partner=partner,
                is_auto=jtp_partner.lenderapproval.is_auto,
                start_date=timezone.localtime(timezone.now()),
                end_date=None,
                delay=jtp_partner.lenderapproval.delay,
                expired_in=jtp_partner.lenderapproval.expired_in,
                expired_start_time=jtp_partner.lenderapproval.expired_start_time,
                expired_end_time=jtp_partner.lenderapproval.expired_end_time,
                is_endless=jtp_partner.lenderapproval.is_endless,
            )


def update_list_lender_info_fs_for_iaf_jtp_lender(apps, schema_editor):
    jtp = LenderCurrent.objects.filter(lender_name="jtp").last()
    if not jtp:
        return

    feature_setting = FeatureSetting.objects.filter(
        feature_name=FeatureNameConst.LIST_LENDER_INFO
    ).last()
    feature_setting.parameters['lenders'][lender_name] = {
        'address': jtp.lender_address,
        'license_no': jtp.license_number,
        'poc_name': jtp.poc_name,
        'poc_position': jtp.poc_position,
        'signature': '',
    }
    feature_setting.save()


def create_new_certificate_for_iaf_jtp_lender(apps, schema_editor):
    lender = LenderCurrent.objects.get_or_none(lender_name=lender_name)
    if lender:
        generate_lender_signature(lender=lender, signature_key=signature_key)


def update_signature_key_configuration_for_iaf_jtp_lender(apps, schema_editor):
    feature_setting = FeatureSetting.objects.filter(
        feature_name=FeatureNameConst.SIGNATURE_KEY_CONFIGURATION
    ).last()
    if not feature_setting:
        return

    lender = LenderCurrent.objects.get_or_none(lender_name=lender_name)
    if lender:
        feature_setting.parameters['users'][str(lender.user.id)] = signature_key
        feature_setting.save()


class Migration(migrations.Migration):
    dependencies = []

    operations = [
        migrations.RunPython(create_iaf_jtp_lender, migrations.RunPython.noop),
        migrations.RunPython(
            update_list_lender_info_fs_for_iaf_jtp_lender, migrations.RunPython.noop
        ),
        migrations.RunPython(create_new_certificate_for_iaf_jtp_lender, migrations.RunPython.noop),
        migrations.RunPython(
            update_signature_key_configuration_for_iaf_jtp_lender, migrations.RunPython.noop
        ),
    ]
