# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-04-12 04:49
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import WorkflowStatusPath, Workflow
from juloserver.julo.constants import WorkflowConst


def add_status_path_julo_144(apps, _schema_editor):
    workflow = Workflow.objects.get(name=WorkflowConst.JULO_ONE)
    WorkflowStatusPath.objects.get_or_create(
        status_previous=140,
        status_next=144,
        type="happy",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=144,
        status_next=150,
        type="happy",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=144,
        status_next=175,
        type="detour",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=144,
        status_next=135,
        type="graveyard",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=144,
        status_next=133,
        type="graveyard",
        workflow=workflow
    )
    WorkflowStatusPath.objects.get_or_create(
        status_previous=144,
        status_next=137,
        type="graveyard",
        workflow=workflow
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_status_path_julo_144, migrations.RunPython.noop)
    ]
