# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-04-12 07:21
from __future__ import unicode_literals

from django.db import migrations, models

from juloserver.julo.statuses import ApplicationStatusCodes
from juloserver.julo.models import StatusLookup, WorkflowStatusPath, Workflow, WorkflowStatusNode
from juloserver.julo.constants import WorkflowConst

def add_new_status_lookup(apps, schema_editor):
    new_status = 107
    desc = "Offer to Regular"
    
    status_lookup = StatusLookup(status_code=new_status, status=desc)
    status_lookup.save()

    workflow = Workflow.objects.get(name=WorkflowConst.JULO_STARTER)
    WorkflowStatusPath.objects.get_or_create(
        status_previous=120,
        status_next=107,
        type="detour",
        workflow=workflow
    )

    WorkflowStatusNode.objects.get_or_create(
        status_node=107,
        handler="JuloStarter107Handler",
        workflow=workflow
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_new_status_lookup)
    ]
