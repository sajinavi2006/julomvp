# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-08-08 19:57
from __future__ import unicode_literals
from juloserver.julo.constants import ExperimentConst
from django.utils import timezone
from dateutil.relativedelta import relativedelta

from django.db import migrations


def add_mycroft_holdout_experiment(apps, schema_editor):
    ExperimentSetting = apps.get_model("julo", "ExperimentSetting")
    criteria = {
        "last_digit_app_ids": [0]
    }
    current_date = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.MYCROFT_HOLDOUT_EXPERIMENT,
        name="MYCROFT HOLDOUT EXPERIMENT",
        start_date=current_date,
        end_date=current_date + relativedelta(years=2),
        schedule="",
        action="",
        type="fraud",
        criteria=criteria)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_mycroft_holdout_experiment, migrations.RunPython.noop)
    ]
