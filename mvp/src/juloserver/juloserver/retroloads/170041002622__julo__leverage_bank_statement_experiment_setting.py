# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-11-19 16:07
from __future__ import unicode_literals

from dateutil.relativedelta import relativedelta

from django.db import migrations
from django.utils import timezone

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def add_experiment_setting_leverage_bank_statement(apps, schema_editor):
    current_datetime = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=True,
        code=ExperimentConst.LEVERAGE_BANK_STATEMENT_EXPERIMENT,
        name="Leverage Bank Statement Experiment",
        start_date=current_datetime,
        end_date=current_datetime + relativedelta(years=1),
        schedule="",
        action="",
        type="underwriting",
        criteria={
            "credit_limit_constant": 1.5,
            "credit_limit_threshold": 300000,
            "max_limit": 5000000,
            "a/b_test": {
                "per_request": 2,
                "percentage": 50
            }
        }
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_experiment_setting_leverage_bank_statement, migrations.RunPython.noop)
    ]
