# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-08-24 02:55
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from dateutil.relativedelta import relativedelta

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def add_experiment_setting_limit_adjustment_factor(apps, schema_editor):
    current_datetime = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.LIMIT_ADJUSTMENT_FACTOR,
        name="Limit adjustment factor experiment",
        start_date=current_datetime,
        end_date=current_datetime + relativedelta(years=1),
        schedule="",
        action="",
        type="account",
        criteria={
            "holdout": {
                "per_requests": 2,
                "percentage": 50
            }
        }
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_experiment_setting_limit_adjustment_factor, migrations.RunPython.noop)
    ]
