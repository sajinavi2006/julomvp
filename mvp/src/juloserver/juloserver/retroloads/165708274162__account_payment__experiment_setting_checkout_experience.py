# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-07-06 04:45
from __future__ import unicode_literals
from email.mime import nonmultipart

from django.db import migrations

from juloserver.julo.constants import ExperimentConst
from juloserver.julo.models import ExperimentSetting


def load_checkout_experience_experiment_setting(app, schema_editor):
    experiment_data = {
        "code": ExperimentConst.CHECKOUT_EXPERIENCE_EXPERIMENT,
        "name": "Checkout Experience Experiment",
        "type": "collection",
        "start_date": "2022-06-08 00:00:00+00", #it's temporary, still can update on CRM, by user
        "end_date": "2022-06-08 00:00:00+00", #it's temporary, still can update on CRM, by user
        "is_active": False,
        "criteria": {
            "account_id_tail": {
                "control_group": [0, 1, 2, 3],
                "experiment_group_1": [4, 5, 6],
                "experiment_group_2": [7, 8, 9]
            }
        }
    }

    ExperimentSetting.objects.create(**experiment_data)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            load_checkout_experience_experiment_setting,
            migrations.RunPython.noop
        )
    ]
