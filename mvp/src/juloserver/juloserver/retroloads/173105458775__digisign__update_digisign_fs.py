# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-11-08 08:29
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.constants import FeatureNameConst
from juloserver.julo.models import FeatureSetting


def update_feature_setting_digisign(apps, schema_editor):
    fs = FeatureSetting.objects.filter(
        feature_name=FeatureNameConst.DIGISIGN
    ).last()

    if not fs:
        return

    params = fs.parameters
    params.update({
        "borrower_price": 500,
        "lender_price": 1500,
        "transaction_method_settings": [
            {"transaction_method": 1, "is_active": False},
        ],
        "minimum_loan_amount": 500_000,
    })

    fs.save()


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(update_feature_setting_digisign, migrations.RunPython.noop),
    ]
