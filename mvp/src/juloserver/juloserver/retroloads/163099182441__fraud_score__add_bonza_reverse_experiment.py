# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-08-08 19:57
from __future__ import unicode_literals
from juloserver.julo.constants import ExperimentConst
from django.utils import timezone
from dateutil.relativedelta import relativedelta

from django.db import migrations


def add_bonza_reverse_experiment(apps, schema_editor):
    ExperimentSetting = apps.get_model("julo", "ExperimentSetting")
    criteria = {
        "test_group_last_account_id": [5]
    }
    current_date = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=True,
        code=ExperimentConst.BONZA_REVERSE_EXPERIMENT,
        name="Bonza Reverse Experiment",
        start_date=current_date,
        end_date=current_date + relativedelta(years=2),
        schedule="",
        action="",
        type="fraud",
        criteria=criteria)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_bonza_reverse_experiment, migrations.RunPython.noop)
    ]
