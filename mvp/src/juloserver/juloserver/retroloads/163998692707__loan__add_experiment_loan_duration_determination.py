# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-12-20 07:55
from __future__ import unicode_literals

from datetime import datetime

from dateutil.relativedelta import relativedelta
from django.db import migrations

from juloserver.julo.models import ExperimentSetting


def add_loan_duration_determination_to_experiment_setting_table(apps, schema_editor):
    from juloserver.julo.constants import ExperimentConst
    ExperimentSetting.objects.create(
        code=ExperimentConst.LOAN_DURATION_DETERMINATION,
        is_active=False,
        name="Add experiment to loan duration",
        start_date=datetime.now(),
        end_date=datetime.now() + relativedelta(days=30),
        type="loan",
        criteria={
            "customer_id": ["#last:3:0,1,2", "#last:4:3,4"]  # just sample
        }
    )


class Migration(migrations.Migration):
    dependencies = [
    ]

    operations = [
        migrations.RunPython(add_loan_duration_determination_to_experiment_setting_table, migrations.RunPython.noop),
    ]
