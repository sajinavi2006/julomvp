# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-01-27 08:58
from __future__ import unicode_literals

from django.db import migrations
from django.contrib.auth.models import User, Group


def prefilled_payment_void_access_users(apps, schema_editor):
    username_list = [
        'husin.matondang',
        'rhestia',
        'riska.handayani',
        'Amelia02',
        'ira.noviyanti',
        'maya.puspa',
        'yorda'
    ]
    new_group, _ = Group.objects.get_or_create(name='payment_void_access')

    for username in username_list:
        user = User.objects.filter(username=username).last()
        if user:
            user.groups.add(new_group)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(prefilled_payment_void_access_users)
    ]
