# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-09-24 04:11
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.constants import FeatureNameConst
from juloserver.julo.models import FeatureSetting
from juloserver.account.constants import AccountLockReason


def update_product_lock_fs_fraud(apps, _schema_editor):
    fs = FeatureSetting.objects.filter(
        is_active=True,
        feature_name=FeatureNameConst.PRODUCT_LOCK_IN_APP_BOTTOM_SHEET,
    ).last()

    fs.parameters[AccountLockReason.FRAUD_BLOCK] = {
        "body": "Saat ini, akun JULOmu sedang terblokir sementara karena alasan keamanan. \nTapi tenang, CS JULO akan menghubungi kamu untuk konfirmasi. Pastikan nomormu selalu aktif, ya. ",
        "title": "Fitur Tidak Dapat Diakses",
        "button": "Mengerti",
    }

    fs.save(update_fields=["parameters"])


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(update_product_lock_fs_fraud, migrations.RunPython.noop)]
