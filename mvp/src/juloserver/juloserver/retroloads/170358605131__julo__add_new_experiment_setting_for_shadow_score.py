# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-12-26 10:20
from __future__ import unicode_literals

from dateutil.relativedelta import relativedelta
from django.db import migrations
from django.utils import timezone

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def execute(apps, schema_editor):
    now = timezone.localtime(timezone.now())
    if not ExperimentSetting.objects.filter(code=ExperimentConst.SHADOW_SCORE_EXPERIMENT).exists():
        ExperimentSetting.objects.create(
            is_active=False,
            code=ExperimentConst.SHADOW_SCORE_EXPERIMENT,
            name="Shadow Score Experiment",
            start_date=now,
            end_date=now + relativedelta(days=30),
            schedule="",
            action="",
            type="formula",
            criteria={
                'empty': 'empty',
            }
        )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(execute, migrations.RunPython.noop),
    ]
