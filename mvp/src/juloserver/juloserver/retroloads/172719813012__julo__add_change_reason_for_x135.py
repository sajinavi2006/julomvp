# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-04-05 07:54
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import StatusLookup, ChangeReason
from juloserver.julo.statuses import ApplicationStatusCodes


def run(apps, _schema_editor):
    status = StatusLookup.objects.filter(
        status_code=ApplicationStatusCodes.APPLICATION_DENIED
    ).last()
    new_change_reason_list = ['foto tidak senonoh', 'job type blacklisted', 'ineligible lbs']
    for reason in new_change_reason_list:
        ChangeReason.objects.get_or_create(
            reason=reason,
            status=status,
        )


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(run, migrations.RunPython.noop)]
