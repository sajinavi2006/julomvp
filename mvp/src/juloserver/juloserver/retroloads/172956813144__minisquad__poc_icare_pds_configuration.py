# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-10-22 03:35
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from dateutil.relativedelta import relativedelta
from juloserver.julo.models import ExperimentSetting
from juloserver.minisquad.constants import (
    ExperimentConst,
    BTTCExperiment,
)


def add_poc_c_icare_experiment(apps, schema_editor):
    now = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.NEW_PDS,
        name="POC C Icare",
        start_date=now,
        end_date=now + relativedelta(days=1),
        schedule="",
        action="",
        type="collection",
        criteria={'delay_upload_function': 15},  # in minutes
    )


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(add_poc_c_icare_experiment, migrations.RunPython.noop)]
