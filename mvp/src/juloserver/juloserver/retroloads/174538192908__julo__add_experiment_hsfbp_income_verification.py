# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2025-04-23 04:18
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from datetime import timedelta

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def execute(apps, schema_editor):

    setting = ExperimentSetting.objects.filter(
        code=ExperimentConst.HSFBP_INCOME_VERIFICATION
    ).exists()
    if not setting:
        ExperimentSetting.objects.create(
            code=ExperimentConst.HSFBP_INCOME_VERIFICATION,
            name='HSFBP Income Verification',
            type='Midas HSFBP',
            start_date=timezone.localtime(timezone.now()),
            end_date=timezone.localtime(timezone.now()) + timedelta(30),
            criteria={
                "app_id": [0, 1],
                "x120_hsfbp_expiry": 2,
            },
            is_active=False,
            is_permanent=False,
        )


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(execute, migrations.RunPython.noop)]
