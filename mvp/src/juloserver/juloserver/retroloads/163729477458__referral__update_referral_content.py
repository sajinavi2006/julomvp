# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2021-11-19 04:06
from __future__ import unicode_literals

from django.db import migrations

from juloserver.julo.models import ReferralSystem


def update_referral_content(apps, _schema_editor):
    referral_system = ReferralSystem.objects.filter(name='PromoReferral').first()
    if referral_system:
        extra_data = referral_system.extra_data
        content = extra_data['content']
        content.update({
            'body': 'Ayo buruan undang temanmu pakai kode referral & langsung dapatkan cashback '
                    'instan {} buat kamu dan {} buat temanmu. Dijamin langsung auto cuan!',
            'message': 'Mau kredit limit besar bisa dipakai kapan aja? Aku rekomendasikan '
                       'pakai JULO! Dapatkan CASHBACK {}. Caranya, download JULO klik link '
                       'https://go.onelink.me/zOQD/Referral & daftar pakai kode referral {}'
        })
        extra_data['content'] = content
        referral_system.extra_data = extra_data
        referral_system.save()


class Migration(migrations.Migration):
    dependencies = [
    ]

    operations = [
        migrations.RunPython(update_referral_content, migrations.RunPython.noop),
    ]
