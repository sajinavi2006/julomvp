# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-05-09 03:14
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from datetime import datetime, time
from dateutil.relativedelta import relativedelta
from juloserver.julo.models import ExperimentSetting
from juloserver.minisquad.constants import ExperimentConst


def add_experiment_setting_merge_nc_bucket(apps, schema_editor):
    today = timezone.localtime(timezone.now())
    start_exp = datetime.combine(today, time.min)
    end_exp = datetime.combine(today + relativedelta(days=30), time.min)
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.MERGE_NON_CONTACTED_BUCKET,
        name="Merge NC bucket to normal bucet",
        start_date=start_exp,
        end_date=end_exp,  # just for inisiated
        schedule="",
        action="",
        type="collection",
        criteria={'buckets': ['JULO_B1']},
    )


class Migration(migrations.Migration):

    dependencies = []

    operations = [
        migrations.RunPython(add_experiment_setting_merge_nc_bucket, migrations.RunPython.noop)
    ]
