# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-03-29 03:32
from __future__ import unicode_literals

from dateutil.relativedelta import relativedelta
from django.db import migrations
from django.utils import timezone

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def execute(apps, schema_editor):

    if not ExperimentSetting.objects.filter(code=ExperimentConst.KTP_OCR_EXPERIMENT).exists():

        now = timezone.localtime(timezone.now())
        ExperimentSetting.objects.create(
            is_active=False,
            code=ExperimentConst.KTP_OCR_EXPERIMENT,
            name="KTP OCR Experiment",
            start_date=now,
            end_date=now + relativedelta(days=30),
            schedule="",
            action="",
            type="application",
            criteria={}
        )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            execute, migrations.RunPython.noop
        )
    ]
