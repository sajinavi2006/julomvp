# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-10-07 09:52
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from juloserver.balance_consolidation.constants import FeatureNameConst
from juloserver.julo.models import FeatureSetting


def update_balance_consolidation_fdc_checking_fs(apps, schema_editor):
    fs = FeatureSetting.objects.filter(
        feature_name=FeatureNameConst.BALANCE_CONSOLIDATION_FDC_CHECKING,
    ).last()

    if not fs:
        return

    start_date = timezone.localtime(timezone.now()).strftime("%Y-%m-%d")
    parameters = {
        'start_date': start_date
    }
    fs.update_safely(parameters=parameters)


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            update_balance_consolidation_fdc_checking_fs, migrations.RunPython.noop
        ),
    ]
