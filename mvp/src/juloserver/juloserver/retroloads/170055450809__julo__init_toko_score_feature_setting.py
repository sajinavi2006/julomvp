# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2023-11-21 08:15
from __future__ import unicode_literals

from dateutil.relativedelta import relativedelta
from django.db import migrations
from django.utils import timezone

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def create_data(apps, schema_editor):
    now = timezone.localtime(timezone.now())
    ExperimentSetting.objects.create(
        is_active=False,
        code=ExperimentConst.TOKO_SCORE_EXPERIMENT,
        name="Tokoscore Experiment",
        start_date=now,
        end_date=now + relativedelta(days=30),
        schedule="",
        action="",
        type="formula",
        criteria={
            'threshold': 650,
            'require_match': True,
            'require_active': True
        }
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(
            create_data, migrations.RunPython.noop
        )
    ]
