# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2022-05-22 13:56
from __future__ import unicode_literals

from django.db import migrations
from juloserver.julo.models import Workflow, WorkflowStatusPath
from juloserver.julo.constants import WorkflowConst


def update_workflow_credit_card(apps, _schema_editor):

    # WorkflowStatusPath
    WorkflowStatusPath.objects.filter(
        status_previous=510,
        status_next=511
    ).update(
        status_previous=510,
        status_next=520
    )
    WorkflowStatusPath.objects.filter(
        status_previous=510,
        status_next=513
    ).update(
        status_previous=510,
        status_next=525
    )
    WorkflowStatusPath.objects.filter(
        status_previous=510,
        status_next=514
    ).update(
        status_previous=510,
        status_next=523
    )
    WorkflowStatusPath.objects.filter(
        status_previous=511,
        status_next=512
    ).update(
        status_previous=520,
        status_next=530
    )


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(update_workflow_credit_card, migrations.RunPython.noop),
    ]
