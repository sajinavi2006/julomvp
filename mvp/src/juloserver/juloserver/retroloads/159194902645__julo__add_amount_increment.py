# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2020-03-03 07:23
from __future__ import unicode_literals

from django.conf import settings
import django.core.validators
from django.db import migrations, models
import django.db.models.deletion
import juloserver.julo.models

from juloserver.julo.product_lines import ProductLineManager

from juloserver.julo.models import ProductLine


def retro_amount_increment(apps, _schema_editor):
    
    product_lines = ProductLine.objects.all()

    for prod in product_lines:
        prod_line = ProductLineManager.get_or_none(prod.product_line_code)
        if prod_line:
            prod.amount_increment = prod_line.amount_increment
            prod.save()


class Migration(migrations.Migration):

    dependencies = [
    ]

    operations = [
        migrations.RunPython(retro_amount_increment, migrations.RunPython.noop),
    ]
