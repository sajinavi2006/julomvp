# -*- coding: utf-8 -*-
# Generated by Django 1.9.5 on 2024-11-22 08:36
from __future__ import unicode_literals

from django.db import migrations
from django.utils import timezone
from dateutil.relativedelta import relativedelta

from juloserver.julo.models import ExperimentSetting
from juloserver.julo.constants import ExperimentConst


def execute(apps, schema_editor):

    code = ExperimentConst.SMILE_MAGNIFEYE_LIVENESS_EXPERIMENT
    if not ExperimentSetting.objects.filter(code=code).exists():

        now = timezone.localtime(timezone.now())
        ExperimentSetting.objects.create(
            is_active=True,
            code=code,
            name="Smile MagnifEye Liveness Experiment",
            start_date=now,
            end_date=now + relativedelta(days=30),
            schedule="",
            action="",
            type="application",
            criteria={},
        )


class Migration(migrations.Migration):

    dependencies = []

    operations = [migrations.RunPython(execute, migrations.RunPython.noop)]
